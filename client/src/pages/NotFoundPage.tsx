

/**
 * Cette page s'affiche lorsque la route accédée n'est pas définie.
 * @returns ReactNode
 */
export default function NotFoundPage() {
    return (
        <div>
            
        </div>
    );
}