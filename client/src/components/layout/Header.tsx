
/**
 * The whole app's header. It defines the app layout.
 * @returns HTMLNode
 */
export default function Header() {
    return (
        <header>You must also configure the whole app's header.</header>
    );
}